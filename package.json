{
  "name": "tbox-mono",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "expo":"bun run --filter='@trailer-box/mobile' start",
    "dev": "bun run --filter='*' dev",
    "build": "bun run --filter='*' build",
    "start": "bun run --filter='*' start",
    "dev:fe": "bun run --filter='@trailer-box/admin' dev",
    "dev:be": "bun run --filter='@trailer-box/server' dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
